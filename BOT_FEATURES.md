## Чеклист возможностей бота

### Для обычного пользователя

- [ ] **Регистрация и доступ**
  - Команда `/start`.
  - Кнопка `✅ Отправить приглашение` отправляет заявку в админский чат и создаёт отдельный топик для пользователя.
  - Статусы пользователя: `new` → `pending` → `approved` или `banned`.

- [ ] **Получение списков контактов**
  - Главное меню: кнопка `📦 Получить списки контактов`.
  - Выбор типа базы (Telegram, WhatsApp, Max, Viber, Нельзяграм, ВКонтакте, Одноклассники, Почта).
  - Бот выдаёт пользователю пачку свободных контактов из соответствующего CSV, учитывая:
    - базовый лимит по типу базы;
    - дополнительные лимиты от менеджера.

- [ ] **Отчёт по лидам**
  - Кнопка `📋 Отчёт по лидам`.
  - Пользователь отправляет скриншоты/файлы/текст, выбирает категорию лида.
  - Бот сохраняет лиды в CSV-файлы по типам и шлёт уведомление в админский чат.

- [ ] **Статистика лидов для пользователя**
  - Кнопка `📊 Статистика лидов`.
  - Бот показывает, сколько лидов пользователь сдал за день / вчера / за всё время.

- [ ] **Запрос новых контактов**
  - Кнопка `🆕 Получить новые контакты`.
  - В админский чат отправляется уведомление с ссылкой на топик пользователя.

- [ ] **Поддержка**
  - Кнопка `💬 Написать в поддержку` переводит пользователя в режим поддержки.
  - Все сообщения из этого режима пересылаются в частный топик пользователя в админском чате.
  - Ответы менеджеров из топика уходят обратно пользователю как ответы бота.

- [ ] **Выход из режимов**
  - Кнопка `⬅️ Назад` — выход из режима поддержки / некоторых сценариев.
  - Кнопка `❌ Отмена` — отмена отправки отчёта, ручного добавления/удаления лида и т.п.

### Для админов и менеджеров (группа поддержки)

- [ ] **Админ-панель**
  - Секретная команда `/get_bd_access_9876` в личном чате с ботом открывает админ-меню:
    - `📥 Загрузить Базу данных` — загрузка новых контактов из Excel;
    - `📤 Выкачать Базу данных` — выгрузка баз в Excel;
    - `🗑 Удалить всю базу данных` — очистка всех баз (через подтверждение);
    - `⬅️ Выход из админки`.

- [ ] **Модерация пользователей**
  - В админском чате (SUPPORT_GROUP_ID):
    - `/add` — одобрить пользователя (статус `approved`);
    - `/ban` — забанить пользователя (статус `banned`);
    - `/unban` — снять бан.

- [ ] **Работа с лидами (топик «Лиды авто» — LEADS_TOPIC_ID)**
  - `/add_lead` — ручное добавление лида:
    - бот запрашивает контакт и категорию;
    - сохраняет лид в соответствующий CSV.
  - `/delete_lead` — удаление лида по контакту.
  - `/leadstats` — агрегированная статистика по всем лидам (всего / за сутки / неделю / месяц).
  - Кнопка `📋 Отчёт по лидам` у пользователей + обработчики в админском чате позволяют обрабатывать отчёты и привязывать лиды к базам/источникам.

- [ ] **Статистика по базам**
  - Команда `/stats` в админском чате:
    - количество свободных и общих контактов по каждой базе;
    - сколько контактов выдано за 1 день / неделю / месяц.

- [ ] **Экспорт данных**
  - `/download_db` — полный Excel по всем базам контактов.
  - `/download_lead` — все лиды в одном Excel.
  - `/download_lead_day` — лиды за текущий «день» (по правилу 20:00).
  - `/check_leads` — в топике с пользователем формирует Excel-файлы с лидами конкретного пользователя (за всё время, за вчера, за сегодня).

- [ ] **Управление базой контактов**
  - Кнопка `📥 Загрузить Базу данных`:
    - загрузка Excel с листами по соцсетям;
    - бот читает первый столбец, очищает значения и добавляет новые контакты в соответствующие CSV;
    - дубликаты фильтруются.
  - Кнопка `📤 Выкачать Базу данных`:
    - бот собирает все CSV в один Excel по листам.
  - Кнопка `🗑 Удалить всю базу данных`:
    - через подтверждение создаёт пустые CSV с только заголовками.

- [ ] **Поддержка и ответы из админского чата**
  - Менеджеры отвечают пользователю прямо из топика в админском чате:
    - текст, фото, документы, видео пересылаются ботом пользователю.
  - Команда `/clear` в админском чате — очистка лимитов пользователя на получение контактов.
  - Команда `/contacts` — выдаёт менеджеру краткую справку/линки по контактам пользователя.

### Служебные задачи и диагностика

- [ ] **Отслеживание Flood control Telegram**
  - Все массовые отправки (файлы, сообщения в группу) проходят с задержкой `FLOOD_DELAY`.
  - Есть команды `/get_online`, `/chatid`, служебные сообщения об ошибках.

- [ ] **Экспорт/импорт Excel**
  - Создание Excel-файлов на основе CSV (`_create_full_excel`, `_create_leads_excel`, `_create_daily_leads_excel`, `_create_user_leads_excel`, `_create_user_contacts_excel`).
  - Загрузка Excel в админке обрабатывается в отдельном потоке, чтобы бот не зависал на больших файлах.

---

## Как это устроено внутри (короткая архитектура)

### Основные файлы данных

- **Контакты по базам**: `base_telegram.csv`, `base_whatsapp.csv`, `base_max.csv`, `base_viber.csv`, `base_instagram.csv`, `base_vk.csv`, `base_ok.csv`, `base_email.csv`.
- **Лиды**: по типам в CSV из `LEAD_TYPES` (например, `leads_telegram.csv` и т.п.).
- **Пользователи**:
  - `users.txt` — счётчик всех когда‑либо заходивших пользователей.
  - `user_status.csv` — статусы (`pending` / `approved` / `banned`).
  - `user_limits.csv` — дополнительные лимиты по базам.
- **Поддержка**:
  - `support_topics.csv` — связь `user_id` ↔ `message_thread_id` в группе поддержки.

Все эти пути и настройки заданы в начале `bot.py` в словарях `BASE_TYPES`, `LEAD_TYPES` и константах.

### Регистрация и статусы пользователей

- Обработчик `/start`: `on_start`.
- Статусы читаются/записываются через функции работы с `user_status.csv`.
- Новые пользователи:
  - видят кнопку `✅ Отправить приглашение` (обработчик `on_send_request`);
  - бот создаёт топик в группе поддержки (`SUPPORT_GROUP_ID`), сохраняет `topic_id` в `support_topics.csv` и шлёт туда заявку.
- Админы в топике используют `/add`, `/ban`, `/unban` для смены статуса.

### Выдача контактов из баз

- Главное меню (`get_main_keyboard`) ведёт к кнопке `📦 Получить списки контактов` (`on_get_base`).
- Пользователь выбирает конкретную базу (кнопки из `USER_BUTTON_MAP`, обработчик `on_user_base_choice`).
- Логика:
  - читается соответствующий CSV из `BASE_TYPES`;
  - учитывается лимит по базе + дополнительные лимиты;
  - контакты помечаются как выданные (сохранение `user_id`, `username`, даты в строках CSV).

### Лиды и отчёты

- Лиды:
  - пользователи сдают отчёты (`on_report_start`, `on_report_file`, `on_report_submit`);
  - каждый лид попадает в CSV соответствующего типа (`LEAD_TYPES`) через функции из блока «РАБОТА С ЛИДАМИ».
- Ручное добавление/удаление лида: обработчики `on_add_lead_*` и `on_delete_lead_*` в топике `LEADS_TOPIC_ID`.
- Статистика по лидам:
  - `/leadstats` (глобально по всем лидам);
  - `on_user_lead_stats` (только по одному пользователю);
  - `/check_leads` в топике пользователя — формирует Excel по лидам этого пользователя.

### Поддержка и связка с админским чатом

- Все связи пользователь ↔ топик в SUPPORT_GROUP_ID хранятся в `support_topics.csv`.
- Пользователь:
  - жмёт `💬 Написать в поддержку` (`on_support_info`, состояние `SupportStates.active`);
  - все его сообщения в этом состоянии обрабатывает `on_user_message_to_support` и пересылает в топик.
- Менеджер:
  - пишет ответ прямо в топике;
  - обработчик `on_support_admin_reply` пересылает ответ к пользователю.

### Админка и управление БД

- Секретная команда `ADMIN_SECRET_COMMAND` открывает админ-меню (`on_admin_command` + `get_admin_keyboard`).
- Загрузка Excel:
  - `on_admin_upload_start` → `on_admin_upload_choice` → `on_admin_file_received`;
  - файл скачивается, обрабатывается в `_process_excel_upload_sync` и добавляет новые значения в CSV.
- Выгрузка:
  - `/download_db`, `/download_lead`, `/download_lead_day`, а также кнопка `📤 Выкачать Базу данных`;
  - функции создания Excel находятся в блоке `СОЗДАНИЕ ФАЙЛОВ`.
- Удаление баз:
  - `on_admin_delete_start` → `on_admin_delete_confirm` / `on_admin_delete_cancel`;
  - реализация через `clear_all_databases()`.

### Запуск

- Точка входа — `main()` в конце `bot.py`.
- Загружает `.env`, читает `BOT_TOKEN`, создаёт CSV при необходимости и запускает `Dispatcher` с регистрацией всех хендлеров.
- На сервере бот работает как `systemd`‑сервис `bot.service` (подробности в `DEPLOY_GUIDE.txt`).

